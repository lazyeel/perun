[package]
name = "perun-shims"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Guest API translation shims for the Perun native runtime: Win32 to POSIX and macOS libSystem/Mach to Linux"

[dependencies]
perun-core = { path = "../perun-core" }
libc = "0.2"
linkme = "0.3"
